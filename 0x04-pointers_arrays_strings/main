#include "holberton.h"
#include <stdio.h>
/*
 * main - check function.
 * Return: 0 on success.
 */

int main(void)
{
	int n;

	n = 402;
	printf("n=%d\n", n);
	reset_to_98(&n);
	printf("n=%d\n", n);
	return (0);
}
